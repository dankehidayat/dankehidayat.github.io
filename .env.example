# last.fm now-playing — read by api/now-playing.mjs at runtime on Vercel
# Create a free API key at https://www.last.fm/api/account/create
# Add these same variables to your Vercel project settings (Environment Variables).
LASTFM_API_KEY=your_lastfm_api_key
LASTFM_USERNAME=your_lastfm_username
